FUSION DROP BUG FIX LOG
========================
2026-07-26 22:50 - STARTING

CRITICAL BUGS:
1. Game freezes after 1st drop on mobile
2. Desktop layout showing on phone
3. Shapes stuck at top, not falling
4. Merging broken
5. Level up impossible

FIXES APPLIED TODAY:
- Floor physics raised 12px
- Duplicate death line removed
- Cache bust v9
- Touch events rewritten
- Mobile UI v3 (bottom panel)
- Debug logging added/removed
- Shape sizes increased 50%

NEXT FIXES NEEDED:
- Game state machine (isAiming/canDrop/justDropped)
- Canvas sizing on mobile
- Physics gravity/acceleration
- Merge detection distance
- Death line logic

2026-07-26 23:25 - DEPLOYED FIXES:
- Canvas sizing simplified (removed complex aspect ratio)
- Physics bug: 'r' was undefined, fixed to e.radius
- Wall collisions restored
- Entity spawn at death line instead of y=-50
- Canvas draw uses actual width/height
- Game.js uses this.canvas.width/height instead of CANVAS_W/H

STILL BROKEN:
- Mobile CSS media query not matching (showing desktop layout)
- Need to verify game flow works end-to-end
- Touch events may still have issues
- Backend APIs returning 404 (expected, no backend deployed)

2026-07-26 23:33 - CRITICAL FIX DEPLOYED:
- #start-screen had NO hidden class, covering everything!
- Added class="overlay hidden" to start-screen in index.html
- This was why user couldn't even start game
